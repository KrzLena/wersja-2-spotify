<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="OdtwarzaczMuzyki.MainPage"
    BackgroundColor="#0F1020"
    Shell.NavBarIsVisible="False"
    Padding="0">

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="64"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Padding="12" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Label Text="Player Title"
                   VerticalOptions="Center"
                   FontSize="24"
                   TextColor="White"/>

            <Button x:Name="AddSongButton"
                    Grid.Column="1"
                    Text="+"
                    WidthRequest="44"
                    HeightRequest="44"
                    VerticalOptions="Center"
                    Clicked="Add_Button_Clicked"/>
        </Grid>

        <SearchBar x:Name="SearchBar"
                   Grid.Row="1"
                   Placeholder="Szukaj"
                   TextChanged="SearchBar_TextChanged"
                   Margin="10"
                   BackgroundColor="#23243A"
                   TextColor="White"/>

        <CollectionView x:Name="SongsCollectionView"
                        Grid.Row="2"
                        SelectionMode="Single"
                        SelectionChanged="SongsCollectionView_SelectionChanged"
                        Margin="8"
                        ItemsLayout="VerticalGrid,2">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame HasShadow="False"
                           CornerRadius="12"
                           Padding="0"
                           Margin="8"
                           BackgroundColor="#2F2E46">
                        <Grid>
                            <Image Source="{Binding ArtworkPath}" Aspect="AspectFill" HeightRequest="160" WidthRequest="160"/>
                            <BoxView BackgroundColor="#00000080"/>
                            <Label Text="{Binding Title}"
                                   HorizontalOptions="Start"
                                   VerticalOptions="End"
                                   Margin="8"
                                   TextColor="White"
                                   FontAttributes="Bold"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Frame Grid.Row="3"
               Margin="10"
               Padding="10"
               BackgroundColor="#2F2E46"
               CornerRadius="12">
            <Label x:Name="CurrentSongLabel"
                   Text="Brak odtwarzania"
                   FontSize="16"
                   HorizontalOptions="Center"
                   TextColor="White"/>
        </Frame>

        <StackLayout Grid.Row="4" Margin="10" Spacing="8">
            <Slider x:Name="PositionSlider"
                    Minimum="0"
                    Maximum="1"
                    ValueChanged="PositionSlider_ValueChanged"
                    DragStarted="PositionSlider_DragStarted"
                    DragCompleted="PositionSlider_DragCompleted"/>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button x:Name="PrevButton" Grid.Column="0" Text="⏮" Clicked="PrevButton_Clicked"/>
                <Button x:Name="PlayButton" Grid.Column="1" Text="▶️" Clicked="PlayButton_Clicked"/>
                <Button x:Name="PauseButton" Grid.Column="2" Text="⏸" Clicked="PauseButton_Clicked"/>
                <Button x:Name="NextButton" Grid.Column="3" Text="⏭" Clicked="NextButton_Clicked"/>
            </Grid>
        </StackLayout>

        <Grid Grid.Row="5" BackgroundColor="#5E5AC8" Padding="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Text="🔖" BackgroundColor="Transparent" TextColor="White"/>
            <Button Grid.Column="1" Text="🏠" BackgroundColor="Transparent" TextColor="White"/>
            <Button Grid.Column="2" Text="＋" BackgroundColor="Transparent" TextColor="White"/>
        </Grid>

        <MediaElement x:Name="Player"
                      IsVisible="False"
                      AutoPlay="False"
                      MediaEnded="Player_MediaEnded"
                      MediaOpened="Player_MediaOpened"
                      PositionChanged="Player_PositionChanged"/>
    </Grid>
</ContentPage>
